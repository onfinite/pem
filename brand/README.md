# Pem brand reference

Static **brand kit** (colors, type, logo rules, voice): open **`pem-brand.html`** in a browser.

It is the visual source of truth when app tokens in `mobile-app/constants/theme.ts` / `typography.ts` need alignment or when copy tone is unclear. Repo-root **`README.md`** summarizes product; this folder is for **design**.
