/** Aligned with backend `CreateDumpDto` and `DUMP_TRANSCRIPT_MAX_CHARS`. */
export const DUMP_TRANSCRIPT_MAX_CHARS = 16_000;

/** Hub + list API page size (backend caps at 50). */
export const PREP_PAGE_SIZE = 12;

/** Settings → “What Pem knows” profile facts list (backend caps at 50). */
export const PROFILE_FACTS_PAGE_SIZE = 20;
