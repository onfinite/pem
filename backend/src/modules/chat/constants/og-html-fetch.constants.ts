/** Max HTML bytes read into memory for OG parsing (head + early body). */
export const OG_HTML_FETCH_MAX_BYTES = 512_000;

/** Abort direct HTML fetch after this. */
export const OG_HTML_FETCH_TIMEOUT_MS = 12_000;
